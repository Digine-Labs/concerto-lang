[project]
name = "schema_retry_fallback"
version = "0.1.0"
entry = "src/main.conc"

[connections.openai]
provider = "openai"
api_key_env = "OPENAI_API_KEY"
default_model = "gpt-4o-mini"

[connections.openai.models]
cheap = "gpt-4o-mini"
robust = "gpt-4o"
